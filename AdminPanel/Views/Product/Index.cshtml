@model IEnumerable<ProductViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
<<<<<<< HEAD
}
=======
}   
>>>>>>> fbc115c (updateRepo)

<h1>Products</h1>
<div>
    <a class="btn btn-outline-primary m-2" asp-action="Create">Add Product</a>
</div>
@if (Model.Any())
{
    <table class="table table-striped table-bordered dt-responsive nowrap ">
        <thead>
            <th>@Html.DisplayNameFor(p=>p.Id)</th>
            <th>Picture</th>
            <th>@Html.DisplayNameFor(p=>p.Name)</th>
            <th>@Html.DisplayNameFor(p=>p.Description)</th>
            <th>@Html.DisplayNameFor(p=>p.Price)</th>
            <th>Brand</th>
            <th>Type</th>
            <th>Action</th>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                <td>@item.Id</td>
                <td>
                 <img class="image-fluid rounded"   src="~/@item.PictureUrl" height="50" width="50" />
                </td>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Price</td>
                <td>@item.ProductBrand.Name</td>
                <td>@item.ProductType.Name</td>
                <td>
                <div class=" d-flex">
                    <a class="btn mb-2 text-warning" asp-action="Edit" asp-route-id="@item.Id"><i class="fas fa-pen"></i> </a>
                    <a class="btn text-danger" asp-action="Delete" asp-route-id="@item.Id"><i class="fas fa-trash"></i></a>
                </div>
                </td>
                </tr>
            }
        </tbody>

    </table>
    
}else
{
   <div class="alert-warning">
       <h2>No products found</h2>
   </div>     
}


